# Copy to .env (gitignored). The chat server reads this at startup.
ANTHROPIC_API_KEY=
# Only needed when the key is an org-level key rather than a workspace-scoped one
# (the API then answers 400 asking for anthropic-workspace-id). Console → Settings → Workspaces.
# ANTHROPIC_WORKSPACE_ID=
# PORT=8787
